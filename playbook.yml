# Test case
---
- hosts: localhost
  tasks:

  - name: Check linux destributive
    debug: var=ansible_os_family


  - name: Install php+nginx+mysql+redis+phpmyadmin+redisadmin
    become: yes
    apt: 
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
      - { name: 'php', state: 'present' }
      - { name: 'nginx', state: 'present' }
      - { name: 'phpmyadmin', state: 'present' }
      - { name: 'redis-server', state: 'present' }    
      - { name: 'mysql-server', state: 'present' }
#      name={{ item }} 
#      state=present
#    with_items:
#        - php
#        - nginx
#        - phpmyadmin
#        - redis-server
#        - mysql-server
