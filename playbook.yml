# Test case
---
- hosts: localhost
  tasks:

  - name: Check linux destributive
    debug: var=ansible_os_family


  - name: Install php+nginx+mysql+redis+phpmyadmin+redisadmin
    become: yes
    apt: 
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
      - { name: 'php', groups: 'present' }
      - { name: 'nginx', groups: 'present' }
      - { name: 'phpmyadmin', groups: 'present' }
      - { name: 'redis-server', groups: 'present' }    
      - { name: 'mysql-server', groups: 'present' }
#      name={{ item }} 
#      state=present
#    with_items:
#        - php
#        - nginx
#        - phpmyadmin
#        - redis-server
#        - mysql-server
